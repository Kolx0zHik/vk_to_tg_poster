services:
  vk2tg:
    build: .
    image: vk_to_tg_poster:dev
    container_name: vk_to_tg_poster_dev
    restart: unless-stopped
    ports:
      - "8222:8222"
    volumes:
      - ./data:/app/data
